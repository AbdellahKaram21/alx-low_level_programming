#include "main.h"

/**
 * _isupper - check if с is upper
 *
 * @c: input for alphabet
 *
 * Return: 1 if its uppercase, 0 if not
*/

int _isupper(int c)
{
	if (с >= 65 && C <= 90)
		return (1);
	else
		return (0);
}

